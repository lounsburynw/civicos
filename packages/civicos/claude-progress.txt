
## Session 199: regenerate_vector_index (2025-12-07)

### Completed
- Verified vector index regenerated during Session 198 (Dec 7 20:36:45)
- Collections confirmed: city-san-rafael_decisions (8 docs), city-san-rafael_chunks (724 docs)
- Cleaned up legacy merrydale_decisions and merrydale_chunks collections
- Tested semantic search via Civic API - what_happened('homeless shelter') returns 8 decisions
- All 113 RAG integration tests pass
- Marked regenerate_vector_index as tested in integration.json

### Key Verifications
- CivicEmbeddings initializes correctly with jurisdiction_id='city-san-rafael'
- ChromaDB database at data/pilot/vectors/city-san-rafael/ (60.4 MB)
- Search latency within acceptable range (~30-100ms after model warmup)
- Deprecation warnings properly emitted for legacy MerrydaleEmbeddings usage

### Next
- architecture_cleanup section complete
- Consider: profile_slow_tests (priority 3), test_markers (priority 3), or video_transcript_rag items

## Session 326: legislative_cron (2025-12-21)

### Completed
- Created `civic-extract legislative` CLI command for weekly legislative context refresh
- Follows seeclickfix.py pattern with full feature parity
- Supports --topic (housing/transportation/environment/budget/education/all)
- Supports --state (default: california), --days-back (default: 90), --limit (default: 20)
- Supports --schedule flag for weekly Sunday 6am schedule
- Supports --dry-run for validation
- Checkpoint save/resume support
- Load_dotenv integration for API key management

### Files Created/Modified
- `packages/civic-extraction/src/civic_extraction/cli/legislative.py` - New CLI module
- `packages/civic-extraction/src/civic_extraction/cli/__init__.py` - Registered legislative subcommand

### Test Results
- Dry-run: Configuration validated, API keys detected ✓
- Full fetch: 30 bills found, 2 locally-relevant bills identified ✓
- Output saved to data/legislative/ and data/legislative_context/ ✓

### Pilot Progress
- 126/161 items ready (78%)
- 35 items remaining

**Commit:** Session 326: legislative_cron - Legislative refresh CLI for weekly context updates

---

## Session 400 - 2025-12-29

### P0 Completed: municipal_code_e2e_cloud

**Goal:** Verify municipal code data exists in cloud Postgres (not migrated - fetched fresh from Municode API).

**Implementation:**
1. Added `municipal_code` table to PostgresBackend schema with temporal versioning
2. Implemented storage methods:
   - `store_municipal_code()` - atomic batch insert with version control
   - `get_municipal_code()` - retrieve with temporal filtering
   - `get_municipal_code_section()` - get specific section
   - `get_municipal_code_count()` - count current sections
3. Created new CLI command: `civic-extract municipal-code`
   - `--jurisdiction city-san-rafael` - target city
   - `--cloud` - store to Postgres
   - `--stats --cloud` - show section count
   - `--dry-run` - validate configuration
   - `--limit N` - for testing

**Results:**
- 2,366 municipal code sections from Municode API stored to Postgres
- Fetch time: ~12 seconds from API
- Store time: ~3 minutes to Postgres
- Free API (no cost tracking needed)

### Files Modified
- `packages/civic/src/civic/storage/postgres_backend.py` - added municipal_code table and methods
- `packages/civic-extraction/src/civic_extraction/cli/municipal_code.py` - new CLI command
- `packages/civic-extraction/src/civic_extraction/cli/__init__.py` - registered command

### Next P0
- vectors_e2e_cloud (build pgvector indexes from cloud SQL data)

## Session 545 (2026-02-03) - Unified City Deployment

**Item:** `unified_city_deployment` (P0)
**Status:** COMPLETE

### What Was Done
Implemented unified config-driven workflow for deploying new cities with a single YAML config.

1. **Unified Config Schema** (`data/jurisdictions/schema.yaml`)
   - Documents all fields: identity, hierarchy, contact, governance, data_sources, financial, geography, tools, Modal config
   - Merges extraction config (JSON) and MCP config (YAML) into single source

2. **Jurisdiction Configs** (`data/jurisdictions/`)
   - `city-san-rafael.yaml` - primary pilot city (ProudCity platform)
   - `city-berkeley.yaml` - second city for validation (Granicus platform)
   - `state-california.yaml` - state-level config
   - `country-united-states.yaml` - federal-level config

3. **Unified Config Loader** (`packages/civicos/src/civicos/jurisdiction_config.py`)
   - `JurisdictionConfig` dataclass with all fields
   - `load_jurisdiction_config()` - loads from YAML
   - `get_active_jurisdictions()` - lists all configs
   - `to_extraction_dict()` - backwards compatibility with extraction code
   - Legacy properties: `source_id`, `source_type`, `base_url`, `archives`

4. **Deploy CLI Command** (`packages/civicos/src/civicos/cli.py`)
   - `civicos-deploy city-berkeley` - full deploy workflow
   - `--dry-run` - show what would be done
   - `--skip-modal` - config validation only
   - `--skip-ingest` - deploy Modal but skip ingestion
   - `--verbose` - detailed progress
   - `--json` - machine-readable output

### Usage
```bash
# Validate config
civicos-deploy city-berkeley --dry-run --verbose

# Full deploy (Modal + ingestion)
civicos-deploy city-berkeley

# Deploy Modal only (no ingestion)
civicos-deploy city-berkeley --skip-ingest
```

### Files Created
- `data/jurisdictions/schema.yaml` - unified schema documentation
- `data/jurisdictions/city-san-rafael.yaml` - San Rafael unified config
- `data/jurisdictions/city-berkeley.yaml` - Berkeley unified config
- `data/jurisdictions/state-california.yaml` - California unified config
- `data/jurisdictions/country-united-states.yaml` - Federal unified config
- `packages/civicos/src/civicos/jurisdiction_config.py` - config loader

### Files Modified
- `packages/civicos/src/civicos/cli.py` - added deploy_main() and run_deploy()
- `packages/civicos/pyproject.toml` - added civicos-deploy entry point
- `pilot.json` - marked unified_city_deployment as ready

### Remaining Work
- Automatic Modal secret creation (currently manual)
- Granicus extractor implementation for Berkeley

### Tests
- 42 smoke tests passed
- Manual testing of civicos-deploy with --dry-run

### Next P0
- TBD (need to set in pilot.json before ending session)

## Session — 2026-03-18 — Federal Funding Pipeline (USAspending)

**Completed:** federal_funding_pipeline (P0)

**What was done:**
- Added `FEDERAL_AWARDS` to CorpusType enum and CORPUS_REGISTRY for vector indexing
- Added `usaspending` config section to jurisdiction extraction configs (san-rafael, city-mill-valley, county-marin)
- Supports `search_names` (multiple name variants), `allowed_names` (false positive filtering), and `recipient_uei`
- Created `fetch_federal_awards()` Modal function in modal_ingest.py with:
  - Multi-jurisdiction iteration via get_active_jurisdictions()
  - Multiple name searches with deduplication by award_id
  - False positive filtering (e.g., "COUNTY OF MARINETTE" excluded from county-marin results)
  - Jurisdiction normalization with graceful skipping for unregistered IDs
  - Auto-indexing support
- Added `federal_awards` to index_vectors() corpus types for city jurisdictions
- Added `fetch_federal_awards` to scheduled_low_velocity_refresh() (weekly)
- Added --federal-awards CLI flag to modal_ingest.py entrypoint

**Results:**
- city-san-rafael: 7 awards ($18.1M) — Treasury SLFRF, DHS port security, DHS firefighters, DOJ COVID
- city-mill-valley: 3 awards ($63K) — small direct awards
- county-marin: SKIPPED — `county-marin` not in jurisdiction registry (uses `marin-county`)

**Known issues:**
- county-marin jurisdiction ID mismatch: config uses `county-marin`, registry uses `marin-county`
- Some duplicate awards in DB with different USAspending internal IDs (same underlying award)
- Small cities get few direct federal awards — most federal money flows via county pass-through

**Next P0:** congressional_votes — Congress.gov API vote retrieval (sessions 3-4 of federal data plan)
