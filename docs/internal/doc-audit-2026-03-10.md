# Documentation Audit — 2026-03-10

Comprehensive review of public and internal docs against codebase implementation (commits through 8d3ae92).

## Critical Issues (broken/misleading for consumers)

### 1. CLAUDE.md references non-existent `docs/critical/` directory
- **Line 517:** `docs/critical/DEPLOYMENT_GUIDE.md` → should be `docs/internal/deployment.md`
- **Line 541:** `docs/critical/BROWSER_EXTENSION_ARCHITECTURE.md` → should be `docs/public/extension/development.md`
- **Status:** ✅ Fixed

### 2. MCP tool count stale
- **File:** `docs/public/mcp/setup.md:1`
- **Says:** "45+ tools" — actual registry has ~55
- **Also:** `search_budget` listed twice (Core Civic line 31 + Financial line 59)
- **Status:** ✅ Fixed

### 3. Missing: Tiered API access docs for builders
- Code implements 6-tier system (`open` → `admin`) with cumulative tool access per tier
- `docs/public/api.md` shows only 5 tiers (missing `open`), doesn't document which tools unlock at which tier
- A builder can't determine what they'll get without reading source
- **Status:** ✅ Fixed

### 4. Acceptance policy: "disabled by default" misleading
- `relay/overview.md:97` says "acceptance policy is disabled by default"
- Latest commits made signature verification mandatory + rate limiting active
- **Status:** ✅ Fixed

## Gaps for External Consumers

### 5. No relay HTTP endpoint reference
- `relay/overview.md` describes services conceptually but never lists actual HTTP endpoints
- Developers building relay clients don't know the routes
- Need: `POST /coordination/voice`, `GET /coordination/voice/counts/{entity}`, etc.
- **Status:** ✅ Fixed

### 6. No Nostr event schema reference for builders
- Kinds (30800, 30803, 1804, etc.) described conceptually but no copy-pasteable event templates
- Someone building in Go/Rust has no reference for exact tag structure
- **Status:** ✅ Fixed

### 7. Missing: `civicos-client` install/usage instructions
- `docs/public/packages/civicos-client.md` shows class examples but no install instructions
- Not clear if published to npm or local-only
- **Status:** ✅ Fixed

### 8. Missing: Cost registry docs for operators
- `docs/public/cost_registry.yaml` exists, nothing explains it
- Operators can't estimate onboarding costs
- **Status:** ⬜ Deferred (needs operator onboarding runbook)

### 9. `civicos-extraction` package docs — verify completeness
- Recent Granicus parser + config-driven ingestion changes may not be reflected
- **Status:** ✅ Fixed

## Stale Content

### 10. "Chrome Web Store coming soon"
- `docs/public/extension/setup.md:7` — stale language
- **Status:** ✅ Fixed

### 11. Data counts not timestamped
- Multiple docs cite "~98 meetings, ~44 decisions" with no date
- **Status:** ✅ Fixed (added timestamps to api.md)

### 12. AI Proxy endpoint URLs are placeholders
- `docs/public/relay/ai-proxy.md` uses template URLs
- **Status:** ⬜ Deferred (needs actual deployment URL)

## Internal Docs Gaps

### 13. No operational guide for cost tracking
- 7 services instrumented, no runbook for monitoring/thresholds/dashboard
- **Status:** ⬜ Deferred (P1 — create when cost alerting is implemented)

### 14. No vector partitioning operational docs
- Partitioning by corpus_type with deferred HNSW — no rebuild/recovery procedures
- **Status:** ⬜ Deferred (P1 — create operational runbook)

### 15. No attestation/signer operational runbook
- `civicos-signer` exists, no internal doc for running it
- **Status:** ⬜ Deferred (P1 — create when signer is deployed)

### 16. Storage schema potentially stale
- New tables: `coordination_rate_limits`, `coordination_write_metadata`, `coordination_feedback`, `platform_api_keys`, `platform_usage_logs`, `operating_costs`
- **Status:** ✅ Fixed

### 17. `relay-acceptance-policy-spec.md` references removed MCP write tools
- Says "write tools currently gated in MCP" but they were removed entirely
- **Status:** ✅ Fixed
