# CivicOS MCP Server Dependencies
#
# Install with: pip install -r requirements-mcp.txt
#
# These are the minimum dependencies needed to run the MCP server.
# For Modal deployment, these are installed in the Modal image (modal_app.py).
# For Docker/container deployment, use this file with pip.

# MCP protocol implementation
mcp[cli]>=1.13.1
fastmcp[code-mode]>=3.1.0

# Database (PostgreSQL with pgvector support)
psycopg2-binary>=2.9.0

# Embeddings for semantic search
fastembed>=0.3.0
numpy<2

# HTTP/async framework
fastapi[standard]>=0.100.0
httpx>=0.24.0
uvicorn>=0.30.0
starlette>=0.38.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0.0

